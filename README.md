# Custom Claude Agents

This is my collection of custom Claude agents. In Claude documentation these are sometimes called subagents.

## How to use 

Each agent is a markdown file. Copy it to your .claude/agents directory and then invoke it like

```
claude --agent <agent-name> --permission-mode bypassPermissions -p '<prompt-for-the-agent>'
```


